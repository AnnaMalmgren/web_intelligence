const BASE_URL = `https://en.wikipedia.org`
const MAX = 300
const SELECTOR = `#mw-content-text`
const DATASET_WORDS = `../server/dataset/Words/`
const DATASET_LINKS = `../server/dataset/Links/`

module.exports = {
  BASE_URL,
  MAX,
  DATASET_LINKS,
  DATASET_WORDS,
  SELECTOR,
}
