'use strict'
const MAX_ITERATIONS = 20
const BASE_URL = `https://en.wikipedia.org`
const DATASET_WORDS = `./dataset/Words/`
const DATASET_LINKS = `./dataset/Links/`

module.exports = {
  MAX_ITERATIONS,
  BASE_URL,
  DATASET_WORDS,
  DATASET_LINKS,
}
